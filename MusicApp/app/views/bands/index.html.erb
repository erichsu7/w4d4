<h1>All bands</h1>
  <ul>
    <% @bands.each do |band| %>
      <li><%= link_to band.name, band_url(band) %></li>
    <% end %>
  </ul>

<%= button_to "Create new band", new_band_url, method: :get %>
